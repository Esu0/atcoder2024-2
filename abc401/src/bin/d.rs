use proconio::input;

fn main() {
    input! {
        n: usize,
        k: usize,
        s_orig: proconio::marker::Bytes,
    }
    // let mut s = vec![b'.'];
    // s.extend(s_orig);
    // s.push(b'.');

    // let mut qs = vec![];
    // let mut cnt = 0;
    // for &si in &s {
        
    // }
}
