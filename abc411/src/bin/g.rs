use proconio::input;
type MInt = util::ModInt<998244353>;

fn main() {
    input! {
        n: usize,
        m: usize,
    }
    let mut mat = vec![vec![0usize; n]; n];
    for _ in 0..m {
        input! { u: usize, v: usize }
        
    }
}
