use proconio::input;
use union_find::UnionFind;

fn main() {
    input! {
        h: usize,
        w: usize,
        f: [[usize; w]; h],
        q: usize,
        abycdz: [((usize, usize), usize, (usize, usize), usize); q],
    }
    
}
